// Read from static external JSON file and push into code

package files;

public class DynamicJson_extJsonFile {
	
	// convert content of file (external JSON file) to String
	// Java method is available to convert content of file to Byte-> then convert Byte to String
	
	// .body(new String(Files.readAllBytes(Paths.get(uri))) where uri = path of JSON file on Windows systems

}
